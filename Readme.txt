
HTML
index.html This page is used to give the file input to the k means application.
clusters.html This page is used to give the number of clusters and the columns on which clustering is to be done.

Python:
The code is in clusterqc.py .

Using Python libraries scikit, which contain k-means cluster
methods, clustering Map-Reduce output (using two dimensional data, or
only two of the dimensions) into clusters of 3, 7, 10, and 20.

